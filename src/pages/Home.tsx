import { IonHeader, IonIcon, IonPage, IonToolbar } from '@ionic/react';
import './Home.scss';

const Home = () => {
  return (
    <IonPage>
      <IonHeader>
        <IonToolbar>Insta KiloGram</IonToolbar>
      </IonHeader>
    </IonPage>
  );
};

export default Home;
