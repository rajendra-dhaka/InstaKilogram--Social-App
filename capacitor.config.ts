import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'social-app-home',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
